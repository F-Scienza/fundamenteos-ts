(()=>{
  let myProductName: string = 'producto 1'
  let myProductPrice: number = 100

  myProductName.toLowerCase()
  myProductPrice.toFixed()
  // cuando llamamos a la variable y escribimos un punto
  // nos ayuda a autocompletar las propiedades, para no equivocarnos

  const myProductStock = 1000
  // cuando es constante no nos dice tipo number
  // nos dice que es el valor ya que no se puede cambiar

})

